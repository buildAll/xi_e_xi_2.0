'use strict';

describe('Service: Tabs', function () {

  // load the service's module
  beforeEach(module('cloudLaundryApp'));

  // instantiate service
  var Tabs;
  beforeEach(inject(function (_Tabs_) {
    Tabs = _Tabs_;
  }));

  it('should do something', function () {
    expect(!!Tabs).toBe(true);
  });

});
